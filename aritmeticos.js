//operadores aritméticos básicos são binários, pois possuem 2 ou mais operandos

const [a,b,c,d] = [3,5,1,15];

const soma = a + b + c;
const subtracao = a - b;
const multiplicacao = b * c;
const divisao = d / a;
const modulo = b % 2; // que seria o resto

console.log(soma, subtracao, multiplicacao, divisao, modulo);